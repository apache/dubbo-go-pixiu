import {request} from './http'; export const opaApi={get:(params:string)=>request<string>('/config/api/opa/policy?'+params),save:(data:{policy_id:string;content:string;server_url?:string;bearer_token?:string})=>request('/config/api/opa/policy',{method:'PUT',body:new URLSearchParams(data)}),remove:(policy_id:string)=>request('/config/api/opa/policy?policy_id='+encodeURIComponent(policy_id),{method:'DELETE'})}
