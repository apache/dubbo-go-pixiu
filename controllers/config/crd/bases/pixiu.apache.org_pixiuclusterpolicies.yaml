---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: pixiuclusterpolicies.pixiu.apache.org
spec:
  group: pixiu.apache.org
  names:
    kind: PixiuClusterPolicy
    listKind: PixiuClusterPolicyList
    plural: pixiuclusterpolicies
    shortNames:
    - pcp
    singular: pixiuclusterpolicy
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PixiuClusterPolicy is the Schema for the pixiuclusterpolicies
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PixiuClusterPolicySpec defines the desired state of PixiuClusterPolicy
            properties:
              clusterRef:
                description: ClusterRef defines a list of clusters and their configurations
                items:
                  description: ClusterConfig defines cluster configuration
                  properties:
                    endpoints:
                      description: Endpoints defines static endpoints (service names
                        will be resolved from k8s endpoints)
                      items:
                        description: EndpointConfig defines a static endpoint
                        properties:
                          address:
                            type: string
                          id:
                            format: int32
                            type: integer
                          port:
                            format: int32
                            type: integer
                          protocolType:
                            type: string
                        required:
                        - address
                        - port
                        type: object
                      type: array
                    loadBalancerPolicy:
                      description: LoadBalancerPolicy specifies the load balancer
                        policy
                      type: string
                    name:
                      description: Name specifies the cluster name
                      type: string
                    type:
                      description: Type specifies the cluster type (static, dynamic,
                        etc.)
                      type: string
                  required:
                  - name
                  type: object
                type: array
              serviceRef:
                description: ServiceRef defines a list of services and their cluster
                  configurations
                items:
                  description: ServiceClusterConfig defines cluster configuration
                    for a service
                  properties:
                    endpoints:
                      description: Endpoints defines static endpoints (overrides Service
                        discovery if specified)
                      items:
                        description: EndpointConfig defines a static endpoint
                        properties:
                          address:
                            type: string
                          id:
                            format: int32
                            type: integer
                          port:
                            format: int32
                            type: integer
                          protocolType:
                            type: string
                        required:
                        - address
                        - port
                        type: object
                      type: array
                    healthCheck:
                      description: HealthCheck defines health check settings
                      properties:
                        healthyThreshold:
                          format: int32
                          type: integer
                        httpHealthCheck:
                          description: HTTPHealthCheckConfig defines HTTP health check
                            settings
                          properties:
                            expectedStatuses:
                              items:
                                format: int32
                                type: integer
                              type: array
                            path:
                              type: string
                          type: object
                        interval:
                          type: string
                        protocol:
                          type: string
                        timeout:
                          type: string
                        unhealthyThreshold:
                          format: int32
                          type: integer
                      type: object
                    loadBalancer:
                      description: LoadBalancer defines load balancing settings
                      properties:
                        policy:
                          type: string
                      type: object
                    name:
                      description: Name specifies the service name (also used as cluster
                        name in conf.yaml)
                      type: string
                    registries:
                      additionalProperties:
                        description: RegistryConfig defines registry configuration
                        properties:
                          address:
                            type: string
                          group:
                            type: string
                          namespace:
                            type: string
                          password:
                            type: string
                          protocol:
                            type: string
                          timeout:
                            type: string
                          username:
                            type: string
                        type: object
                      description: Registries defines registry configurations
                      type: object
                    timeout:
                      description: Timeout defines timeout settings for the cluster
                      properties:
                        connect:
                          type: string
                        request:
                          type: string
                      type: object
                    type:
                      description: Type specifies the cluster type
                      type: string
                  required:
                  - name
                  type: object
                type: array
              targetRef:
                description: TargetRef identifies an API object to apply policy to.
                properties:
                  group:
                    description: Group is the group of the target resource.
                    type: string
                  kind:
                    description: Kind is kind of the target resource.
                    type: string
                  name:
                    description: Name is the name of the target resource.
                    type: string
                  namespace:
                    description: Namespace is the namespace of the target resource.
                    maxLength: 63
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                required:
                - kind
                - name
                type: object
            type: object
          status:
            description: PixiuClusterPolicyStatus defines the observed state of PixiuClusterPolicy
            properties:
              conditions:
                description: Conditions describe the current conditions of the PixiuClusterPolicy.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
